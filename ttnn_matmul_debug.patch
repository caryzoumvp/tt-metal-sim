diff --git a/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/read_tile.cpp b/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/read_tile.cpp
index e9166fbaa7..d7af2f7229 100644
--- a/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/read_tile.cpp
+++ b/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/read_tile.cpp
@@ -4,6 +4,9 @@
 
 #include <cstdint>
 
+#include "api/debug/dprint.h"
+#include "noc_nonblocking_api.h"
+
 void kernel_main() {
     // Read parameters from the kernel arguments
     uint32_t in0_addr = get_arg_val<uint32_t>(0);
@@ -34,7 +37,15 @@ void kernel_main() {
                                                    // We can overlap async reads and writes
                                                    // to reduce the data movement overhead.
 
-        noc_async_read_barrier();  // Wait until tile reads are done
+        DPRINT << "rd_before tile=" << i
+               << " hw_rd_resp=" << NOC_STATUS_READ_REG(1, NIU_MST_RD_RESP_RECEIVED)
+               << " sw_rd_issued=" << noc_reads_num_issued[1]
+               << ENDL();
+        noc_async_read_barrier();
+        DPRINT << "rd_after tile=" << i
+               << " hw_rd_resp=" << NOC_STATUS_READ_REG(1, NIU_MST_RD_RESP_RECEIVED)
+               << " sw_rd_issued=" << noc_reads_num_issued[1]
+               << ENDL();
         cb_push_back(cb_in0, 1);   // mark the tiles as ready. From this point forward kernels
                                    // calling `cb_wait_front` will see this tile
     }
diff --git a/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/write_tile.cpp b/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/write_tile.cpp
index a1a401eb33..896765e75e 100644
--- a/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/write_tile.cpp
+++ b/tt_metal/programming_examples/eltwise_sfpu/kernels/dataflow/write_tile.cpp
@@ -4,6 +4,9 @@
 
 #include <cstdint>
 
+#include "api/debug/dprint.h"
+#include "noc_nonblocking_api.h"
+
 void kernel_main() {
     uint32_t c_addr = get_arg_val<uint32_t>(0);
     uint32_t n_tiles = get_arg_val<uint32_t>(1);
@@ -23,7 +26,17 @@ void kernel_main() {
         uint32_t cb_out0_addr = get_read_ptr(cb_out0);
         // Write the tile to DRAM
         noc_async_write_tile(i, out0, cb_out0_addr);
-        noc_async_write_barrier();  // This will wait until the write is done. As an alternative,
+        DPRINT << "wr_before tile=" << i
+               << " hw_wr_ack=" << NOC_STATUS_READ_REG(0, NIU_MST_WR_ACK_RECEIVED)
+               << " sw_wr_acked=" << noc_nonposted_writes_acked[0]
+               << " hw_wr_sent=" << NOC_STATUS_READ_REG(0, NIU_MST_NONPOSTED_WR_REQ_SENT)
+               << " sw_wr_sent=" << noc_nonposted_writes_num_issued[0]
+               << ENDL();
+        noc_async_write_barrier();
+        DPRINT << "wr_after tile=" << i
+               << " hw_wr_ack=" << NOC_STATUS_READ_REG(0, NIU_MST_WR_ACK_RECEIVED)
+               << " sw_wr_acked=" << noc_nonposted_writes_acked[0]
+               << ENDL();
                                     // noc_async_write_flushed() can be faster because it waits
                                     // until the write request is sent. In that case, you have to
                                     // use noc_async_write_barrier() at least once at the end of
diff --git a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_receiver.cpp b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_receiver.cpp
index b345d12d7f..312cc2e67a 100644
--- a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_receiver.cpp
+++ b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_receiver.cpp
@@ -4,6 +4,7 @@
 
 #include <stdint.h>
 #include "api/dataflow/dataflow_api.h"
+#include "api/debug/dprint.h"
 #include "hostdevcommon/common_values.hpp"
 
 void kernel_main() {
@@ -70,36 +71,58 @@ void kernel_main() {
     volatile tt_l1_ptr uint32_t* in1_mcast_receiver_semaphore_addr_ptr =
         reinterpret_cast<volatile tt_l1_ptr uint32_t*>(in1_mcast_receiver_semaphore_addr);
 
+    DPRINT << "mcast rr sem init in0_sender=" << HEX() << in0_mcast_sender_semaphore_addr << DEC()
+           << " in0_receiver=" << HEX() << in0_mcast_receiver_semaphore_addr << DEC()
+           << " in1_sender=" << HEX() << in1_mcast_sender_semaphore_addr << DEC()
+           << " in1_receiver=" << HEX() << in1_mcast_receiver_semaphore_addr << DEC() << ENDL();
+
     for (uint32_t b = 0; b < batch; b++) {
         for (uint32_t block = 0; block < num_blocks; block++) {
+            DPRINT << "mcast rr block=" << block << " start" << ENDL();
             // Operand 0
+            DPRINT << "mcast rr block=" << block << " in0 reserve start tiles=" << in0_block_num_tiles << ENDL();
             cb_reserve_back(cb_id_in0, in0_block_num_tiles);
+            DPRINT << "mcast rr block=" << block << " in0 reserve done" << ENDL();
 
             // Set in0 semaphore value to INVALID
             noc_semaphore_set(in0_mcast_receiver_semaphore_addr_ptr, INVALID);
-
             // Atomic increment source core counter
             uint64_t in0_mcast_sender_semaphore_noc_addr =
                 get_noc_addr(in0_mcast_sender_noc_x, in0_mcast_sender_noc_y, in0_mcast_sender_semaphore_addr);
+            DPRINT << "mcast rr block=" << block << " in0 notify sender local_recv=" << HEX()
+                   << in0_mcast_receiver_semaphore_addr << " remote_sender=" << in0_mcast_sender_semaphore_noc_addr
+                   << DEC() << ENDL();
             noc_semaphore_inc(in0_mcast_sender_semaphore_noc_addr, 1);
 
             // wait on in0 semaphore value to become VALID (set by mcast sender after it multicasts data)
+            DPRINT << "mcast rr block=" << block << " in0 wait sender recv_sem=" << HEX()
+                   << in0_mcast_receiver_semaphore_addr << DEC() << " current="
+                   << *in0_mcast_receiver_semaphore_addr_ptr << ENDL();
             noc_semaphore_wait(in0_mcast_receiver_semaphore_addr_ptr, VALID);
+            DPRINT << "mcast rr block=" << block << " in0 sender done" << ENDL();
 
             cb_push_back(cb_id_in0, in0_block_num_tiles);
 
             // Operand 1
+            DPRINT << "mcast rr block=" << block << " in1 reserve start tiles=" << in1_block_num_tiles << ENDL();
             cb_reserve_back(cb_id_in1, in1_block_num_tiles);
+            DPRINT << "mcast rr block=" << block << " in1 reserve done" << ENDL();
 
             // Set in1 semaphore value to INVALID
             noc_semaphore_set(in1_mcast_receiver_semaphore_addr_ptr, INVALID);
-
             uint64_t in1_mcast_sender_semaphore_noc_addr =
                 get_noc_addr(in1_mcast_sender_noc_x, in1_mcast_sender_noc_y, in1_mcast_sender_semaphore_addr);
+            DPRINT << "mcast rr block=" << block << " in1 notify sender local_recv=" << HEX()
+                   << in1_mcast_receiver_semaphore_addr << " remote_sender=" << in1_mcast_sender_semaphore_noc_addr
+                   << DEC() << ENDL();
             noc_semaphore_inc(in1_mcast_sender_semaphore_noc_addr, 1);
 
             // wait on in1 semaphore value to become VALID (set by mcast sender after it multicasts data)
+            DPRINT << "mcast rr block=" << block << " in1 wait sender recv_sem=" << HEX()
+                   << in1_mcast_receiver_semaphore_addr << DEC() << " current="
+                   << *in1_mcast_receiver_semaphore_addr_ptr << ENDL();
             noc_semaphore_wait(in1_mcast_receiver_semaphore_addr_ptr, VALID);
+            DPRINT << "mcast rr block=" << block << " in1 sender done" << ENDL();
 
             cb_push_back(cb_id_in1, in1_block_num_tiles);
         }
diff --git a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_sender.cpp b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_sender.cpp
index 5357e5efe1..8a5fb8922c 100644
--- a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_sender.cpp
+++ b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_receiver_in1_sender.cpp
@@ -4,6 +4,7 @@
 
 #include <stdint.h>
 #include "api/dataflow/dataflow_api.h"
+#include "api/debug/dprint.h"
 #include "hostdevcommon/common_values.hpp"
 
 void kernel_main() {
@@ -81,6 +82,11 @@ void kernel_main() {
     volatile tt_l1_ptr uint32_t* in1_mcast_sender_semaphore_addr_ptr =
         reinterpret_cast<volatile tt_l1_ptr uint32_t*>(in1_mcast_sender_semaphore_addr);
 
+    DPRINT << "mcast rs sem init in0_sender=" << HEX() << in0_mcast_sender_semaphore_addr << DEC()
+           << " in0_receiver=" << HEX() << in0_mcast_receiver_semaphore_addr << DEC()
+           << " in1_sender=" << HEX() << in1_mcast_sender_semaphore_addr << DEC()
+           << " in1_receiver=" << HEX() << in1_mcast_receiver_semaphore_addr << DEC() << ENDL();
+
     constexpr auto s0_args = TensorAccessorArgs<0>();
     constexpr auto s1_args = TensorAccessorArgs<s0_args.next_compile_time_args_offset()>();
     const auto s1 = TensorAccessor(s1_args, in1_tensor_addr, single_tile_size_bytes);
@@ -88,24 +94,35 @@ void kernel_main() {
     for (uint32_t b = 0; b < batch; b++) {
         uint32_t in1_tensor_current_block_start_tile_id = in1_tensor_start_tile_id;
         for (uint32_t block = 0; block < num_blocks; block++) {
+            DPRINT << "mcast rs block=" << block << " start" << ENDL();
             // Operand 0
+            DPRINT << "mcast rs block=" << block << " in0 reserve start tiles=" << in0_block_num_tiles << ENDL();
             cb_reserve_back(cb_id_in0, in0_block_num_tiles);
+            DPRINT << "mcast rs block=" << block << " in0 reserve done" << ENDL();
 
             // Set in0 semaphore value to INVALID
             noc_semaphore_set(in0_mcast_receiver_semaphore_addr_ptr, INVALID);
-
             // Atomic increment source core counter
             uint64_t in0_mcast_sender_semaphore_noc_addr =
                 get_noc_addr(in0_mcast_sender_noc_x, in0_mcast_sender_noc_y, in0_mcast_sender_semaphore_addr);
+            DPRINT << "mcast rs block=" << block << " in0 notify sender local_recv=" << HEX()
+                   << in0_mcast_receiver_semaphore_addr << " remote_sender=" << in0_mcast_sender_semaphore_noc_addr
+                   << DEC() << ENDL();
             noc_semaphore_inc(in0_mcast_sender_semaphore_noc_addr, 1);
 
             // wait on in0 semaphore value to become VALID (set by mcast sender after it multicasts data)
+            DPRINT << "mcast rs block=" << block << " in0 wait sender recv_sem=" << HEX()
+                   << in0_mcast_receiver_semaphore_addr << DEC() << " current="
+                   << *in0_mcast_receiver_semaphore_addr_ptr << ENDL();
             noc_semaphore_wait(in0_mcast_receiver_semaphore_addr_ptr, VALID);
+            DPRINT << "mcast rs block=" << block << " in0 sender done" << ENDL();
 
             cb_push_back(cb_id_in0, in0_block_num_tiles);
 
             // Operand 1
+            DPRINT << "mcast rs block=" << block << " in1 reserve start tiles=" << in1_block_num_tiles << ENDL();
             cb_reserve_back(cb_id_in1, in1_block_num_tiles);
+            DPRINT << "mcast rs block=" << block << " in1 reserve done" << ENDL();
             l1_write_addr_in1 = get_write_ptr(cb_id_in1);
 
             uint32_t in1_start_address = l1_write_addr_in1;  // copy start address of block, to be used for mcasting
@@ -128,9 +145,13 @@ void kernel_main() {
             // Barrier! make sure the reads are done
             noc_async_read_barrier();
 
+            DPRINT << "mcast rs block=" << block << " in1 wait receivers sender_sem=" << HEX()
+                   << in1_mcast_sender_semaphore_addr << DEC() << " expect=" << in1_mcast_num_dests
+                   << " current=" << *in1_mcast_sender_semaphore_addr_ptr << ENDL();
             // wait until all in1 mcast destinations have atomically incremented the in1 semaphore_addr (i.e. its value
             // should be in0_mcast_num_dests), then reset the semaphore_addr value back to zero for the next block
             noc_semaphore_wait(in1_mcast_sender_semaphore_addr_ptr, in1_mcast_num_dests);
+            DPRINT << "mcast rs block=" << block << " in1 receivers ready" << ENDL();
             noc_semaphore_set(in1_mcast_sender_semaphore_addr_ptr, 0);
 
             // Now we have the block in the CB address, we can mcast to dests!
@@ -143,6 +164,7 @@ void kernel_main() {
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_async_write_multicast(
                 in1_start_address, in1_multicast_data_addr, in1_block_size_bytes, in1_mcast_num_dests);
+            DPRINT << "mcast rs block=" << block << " in1 data multicast sent" << ENDL();
 
             // Note: no need for write barrier, since these two multicasts are done on the same noc id and same vc even
             // though cmd bufs are different Also, this only works because we are setting VCs statically (using
@@ -160,9 +182,13 @@ void kernel_main() {
                 in1_mcast_dest_noc_end_x,
                 in1_mcast_dest_noc_end_y,
                 in1_mcast_receiver_semaphore_addr);
+            DPRINT << "mcast rs block=" << block << " in1 flag sem local=" << HEX()
+                   << in1_mcast_receiver_semaphore_addr << " remote=" << in1_mcast_receiver_semaphore_noc_addr
+                   << DEC() << " num_dests=" << in1_mcast_num_dests << ENDL();
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_semaphore_set_multicast(
                 in1_mcast_receiver_semaphore_addr, in1_mcast_receiver_semaphore_noc_addr, in1_mcast_num_dests);
+            DPRINT << "mcast rs block=" << block << " in1 flag multicast sent" << ENDL();
 
             cb_push_back(cb_id_in1, in1_block_num_tiles);
         }
diff --git a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_receiver.cpp b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_receiver.cpp
index b74d09d4cf..434aa7c67d 100644
--- a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_receiver.cpp
+++ b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_receiver.cpp
@@ -4,6 +4,7 @@
 
 #include <stdint.h>
 #include "api/dataflow/dataflow_api.h"
+#include "api/debug/dprint.h"
 #include "hostdevcommon/common_values.hpp"
 
 void kernel_main() {
@@ -81,12 +82,18 @@ void kernel_main() {
     volatile tt_l1_ptr uint32_t* in1_mcast_receiver_semaphore_addr_ptr =
         reinterpret_cast<volatile tt_l1_ptr uint32_t*>(in1_mcast_receiver_semaphore_addr);
 
+    DPRINT << "mcast sr sem init in0_sender=" << HEX() << in0_mcast_sender_semaphore_addr << DEC()
+           << " in0_receiver=" << HEX() << in0_mcast_receiver_semaphore_addr << DEC()
+           << " in1_sender=" << HEX() << in1_mcast_sender_semaphore_addr << DEC()
+           << " in1_receiver=" << HEX() << in1_mcast_receiver_semaphore_addr << DEC() << ENDL();
+
     constexpr auto s0_args = TensorAccessorArgs<0>();
     const auto s0 = TensorAccessor(s0_args, in0_tensor_addr, single_tile_size_bytes);
 
     for (uint32_t b = 0; b < batch; b++) {
         uint32_t in0_tensor_current_block_start_tile_id = in0_tensor_start_tile_id;
         for (uint32_t block = 0; block < num_blocks; block++) {
+            DPRINT << "mcast sr block=" << block << " start" << ENDL();
             cb_reserve_back(cb_id_in0, in0_block_num_tiles);
             l1_write_addr_in0 = get_write_ptr(cb_id_in0);
 
@@ -110,9 +117,13 @@ void kernel_main() {
             // Barrier! make sure the reads are done
             noc_async_read_barrier();
 
+            DPRINT << "mcast sr block=" << block << " in0 wait receivers sender_sem=" << HEX()
+                   << in0_mcast_sender_semaphore_addr << DEC() << " expect=" << in0_mcast_num_dests
+                   << " current=" << *in0_mcast_sender_semaphore_addr_ptr << ENDL();
             // wait until all in0 mcast destinations have atomically incremented the in0 semaphore_addr (i.e. its value
             // should be in0_mcast_num_dests), then reset the semaphore_addr value back to zero for the next block
             noc_semaphore_wait(in0_mcast_sender_semaphore_addr_ptr, in0_mcast_num_dests);
+            DPRINT << "mcast sr block=" << block << " in0 receivers ready" << ENDL();
             noc_semaphore_set(in0_mcast_sender_semaphore_addr_ptr, 0);
             // Now we have the block in the CB address, we can mcast to dests!
             uint64_t in0_multicast_data_addr = get_noc_multicast_addr(
@@ -124,6 +135,7 @@ void kernel_main() {
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_async_write_multicast(
                 in0_start_address, in0_multicast_data_addr, in0_block_size_bytes, in0_mcast_num_dests);
+            DPRINT << "mcast sr block=" << block << " in0 data multicast sent" << ENDL();
 
             // Note: no need for write barrier, since these two multicasts are done on the same noc id and same vc even
             // though cmd bufs are different Also, this only works because we are setting VCs statically (using
@@ -141,9 +153,13 @@ void kernel_main() {
                 in0_mcast_dest_noc_start_x,
                 in0_mcast_dest_noc_start_y,
                 in0_mcast_receiver_semaphore_addr);
+            DPRINT << "mcast sr block=" << block << " in0 flag sem local=" << HEX()
+                   << in0_mcast_receiver_semaphore_addr << " remote=" << in0_mcast_receiver_semaphore_noc_addr
+                   << DEC() << " num_dests=" << in0_mcast_num_dests << ENDL();
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_semaphore_set_multicast(
                 in0_mcast_receiver_semaphore_addr, in0_mcast_receiver_semaphore_noc_addr, in0_mcast_num_dests);
+            DPRINT << "mcast sr block=" << block << " in0 flag multicast sent" << ENDL();
 
             cb_push_back(cb_id_in0, in0_block_num_tiles);
 
@@ -152,13 +168,19 @@ void kernel_main() {
 
             // Set in1 semaphore value to INVALID
             noc_semaphore_set(in1_mcast_receiver_semaphore_addr_ptr, INVALID);
-
             uint64_t in1_mcast_sender_semaphore_noc_addr =
                 get_noc_addr(in1_mcast_sender_noc_x, in1_mcast_sender_noc_y, in1_mcast_sender_semaphore_addr);
+            DPRINT << "mcast sr block=" << block << " in1 notify sender local_recv=" << HEX()
+                   << in1_mcast_receiver_semaphore_addr << " remote_sender=" << in1_mcast_sender_semaphore_noc_addr
+                   << DEC() << ENDL();
             noc_semaphore_inc(in1_mcast_sender_semaphore_noc_addr, 1);
 
             // wait on in1 semaphore value to become VALID (set by mcast sender after it multicasts data)
+            DPRINT << "mcast sr block=" << block << " in1 wait sender recv_sem=" << HEX()
+                   << in1_mcast_receiver_semaphore_addr << DEC() << " current="
+                   << *in1_mcast_receiver_semaphore_addr_ptr << ENDL();
             noc_semaphore_wait(in1_mcast_receiver_semaphore_addr_ptr, VALID);
+            DPRINT << "mcast sr block=" << block << " in1 sender done" << ENDL();
 
             cb_push_back(cb_id_in1, in1_block_num_tiles);
         }
diff --git a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_sender.cpp b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_sender.cpp
index 0a36348e32..5660eb3638 100644
--- a/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_sender.cpp
+++ b/tt_metal/programming_examples/matmul/matmul_common/kernels/dataflow/reader_bmm_tile_layout_in0_sender_in1_sender.cpp
@@ -4,6 +4,7 @@
 
 #include <stdint.h>
 #include "api/dataflow/dataflow_api.h"
+#include "api/debug/dprint.h"
 #include "hostdevcommon/common_values.hpp"
 
 void kernel_main() {
@@ -92,6 +93,11 @@ void kernel_main() {
     volatile tt_l1_ptr uint32_t* in1_mcast_sender_semaphore_addr_ptr =
         reinterpret_cast<volatile tt_l1_ptr uint32_t*>(in1_mcast_sender_semaphore_addr);
 
+    DPRINT << "mcast ss sem init in0_sender=" << HEX() << in0_mcast_sender_semaphore_addr << DEC()
+           << " in0_receiver=" << HEX() << in0_mcast_receiver_semaphore_addr << DEC()
+           << " in1_sender=" << HEX() << in1_mcast_sender_semaphore_addr << DEC()
+           << " in1_receiver=" << HEX() << in1_mcast_receiver_semaphore_addr << DEC() << ENDL();
+
     constexpr auto s0_args = TensorAccessorArgs<0>();
     const auto s0 = TensorAccessor(s0_args, in0_tensor_addr, single_tile_size_bytes);
     constexpr auto s1_args = TensorAccessorArgs<s0_args.next_compile_time_args_offset()>();
@@ -101,6 +107,7 @@ void kernel_main() {
         uint32_t in0_tensor_current_block_start_tile_id = in0_tensor_start_tile_id;
         uint32_t in1_tensor_current_block_start_tile_id = in1_tensor_start_tile_id;
         for (uint32_t block = 0; block < num_blocks; block++) {
+            DPRINT << "mcast ss block=" << block << " start" << ENDL();
             cb_reserve_back(cb_id_in0, in0_block_num_tiles);
             l1_write_addr_in0 = get_write_ptr(cb_id_in0);
 
@@ -124,9 +131,13 @@ void kernel_main() {
             // Barrier! make sure the reads are done
             noc_async_read_barrier();
 
+            DPRINT << "mcast ss block=" << block << " in0 wait receivers sender_sem=" << HEX()
+                   << in0_mcast_sender_semaphore_addr << DEC() << " expect=" << in0_mcast_num_dests
+                   << " current=" << *in0_mcast_sender_semaphore_addr_ptr << ENDL();
             // wait until all in0 mcast destinations have atomically incremented the in0 semaphore_addr (i.e. its value
             // should be in0_mcast_num_dests), then reset the semaphore_addr value back to zero for the next block
             noc_semaphore_wait(in0_mcast_sender_semaphore_addr_ptr, in0_mcast_num_dests);
+            DPRINT << "mcast ss block=" << block << " in0 receivers ready" << ENDL();
             noc_semaphore_set(in0_mcast_sender_semaphore_addr_ptr, 0);
 
             // Now we have the block in the CB address, we can mcast to dests!
@@ -139,6 +150,7 @@ void kernel_main() {
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_async_write_multicast(
                 in0_start_address, in0_multicast_data_addr, in0_block_size_bytes, in0_mcast_num_dests);
+            DPRINT << "mcast ss block=" << block << " in0 data multicast sent" << ENDL();
 
             // Note: no need for write barrier, since these two multicasts are done on the same noc id and same vc even
             // though cmd bufs are different Also, this only works because we are setting VCs statically (using
@@ -156,9 +168,13 @@ void kernel_main() {
                 in0_mcast_dest_noc_start_x,
                 in0_mcast_dest_noc_start_y,
                 in0_mcast_receiver_semaphore_addr);
+            DPRINT << "mcast ss block=" << block << " in0 flag sem local=" << HEX()
+                   << in0_mcast_receiver_semaphore_addr << " remote=" << in0_mcast_receiver_semaphore_noc_addr
+                   << DEC() << " num_dests=" << in0_mcast_num_dests << ENDL();
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_semaphore_set_multicast(
                 in0_mcast_receiver_semaphore_addr, in0_mcast_receiver_semaphore_noc_addr, in0_mcast_num_dests);
+            DPRINT << "mcast ss block=" << block << " in0 flag multicast sent" << ENDL();
 
             cb_push_back(cb_id_in0, in0_block_num_tiles);
 
@@ -186,9 +202,13 @@ void kernel_main() {
             // Barrier! make sure the reads are done
             noc_async_read_barrier();
 
+            DPRINT << "mcast ss block=" << block << " in1 wait receivers sender_sem=" << HEX()
+                   << in1_mcast_sender_semaphore_addr << DEC() << " expect=" << in1_mcast_num_dests
+                   << " current=" << *in1_mcast_sender_semaphore_addr_ptr << ENDL();
             // wait until all in1 mcast destinations have atomically incremented the in1 semaphore_addr (i.e. its value
             // should be in0_mcast_num_dests), then reset the semaphore_addr value back to zero for the next block
             noc_semaphore_wait(in1_mcast_sender_semaphore_addr_ptr, in1_mcast_num_dests);
+            DPRINT << "mcast ss block=" << block << " in1 receivers ready" << ENDL();
             noc_semaphore_set(in1_mcast_sender_semaphore_addr_ptr, 0);
 
             // Now we have the block in the CB address, we can mcast to dests!
@@ -201,6 +221,7 @@ void kernel_main() {
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_async_write_multicast(
                 in1_start_address, in1_multicast_data_addr, in1_block_size_bytes, in1_mcast_num_dests);
+            DPRINT << "mcast ss block=" << block << " in1 data multicast sent" << ENDL();
 
             // Note: no need for write barrier, since these two multicasts are done on the same noc id and same vc even
             // though cmd bufs are different Also, this only works because we are setting VCs statically (using
@@ -218,9 +239,13 @@ void kernel_main() {
                 in1_mcast_dest_noc_start_x,
                 in1_mcast_dest_noc_start_y,
                 in1_mcast_receiver_semaphore_addr);
+            DPRINT << "mcast ss block=" << block << " in1 flag sem local=" << HEX()
+                   << in1_mcast_receiver_semaphore_addr << " remote=" << in1_mcast_receiver_semaphore_noc_addr
+                   << DEC() << " num_dests=" << in1_mcast_num_dests << ENDL();
             // num_dests must not include source, since we are NOT really doing a local copy!
             noc_semaphore_set_multicast(
                 in1_mcast_receiver_semaphore_addr, in1_mcast_receiver_semaphore_noc_addr, in1_mcast_num_dests);
+            DPRINT << "mcast ss block=" << block << " in1 flag multicast sent" << ENDL();
 
             cb_push_back(cb_id_in1, in1_block_num_tiles);
         }
diff --git a/tt_metal/programming_examples/matmul/matmul_single_core/matmul_single_core.cpp b/tt_metal/programming_examples/matmul/matmul_single_core/matmul_single_core.cpp
index 606b641589..c9d4622194 100644
--- a/tt_metal/programming_examples/matmul/matmul_single_core/matmul_single_core.cpp
+++ b/tt_metal/programming_examples/matmul/matmul_single_core/matmul_single_core.cpp
@@ -191,9 +191,9 @@ int main() {
         std::shared_ptr<distributed::MeshDevice> mesh_device = distributed::MeshDevice::create_unit_mesh(device_id);
 
         // parameters for the matrix multiplication
-        constexpr uint32_t M = 640;  // user-defined
-        constexpr uint32_t N = 640;  // user-defined
-        constexpr uint32_t K = 640;  // user-defined
+        constexpr uint32_t M = 128;  // user-defined
+        constexpr uint32_t N = 128;  // user-defined
+        constexpr uint32_t K = 128;  // user-defined
 
         static_assert(M % TILE_HEIGHT == 0, "M must be divisible by TILE_HEIGHT");
         static_assert(N % TILE_WIDTH == 0, "N must be divisible by TILE_WIDTH");
diff --git a/ttnn/cpp/ttnn/operations/matmul/device/kernels/compute/bmm_large_block_zm_fused_bias_activation.cpp b/ttnn/cpp/ttnn/operations/matmul/device/kernels/compute/bmm_large_block_zm_fused_bias_activation.cpp
index bfeb8fe393..f8ea43a36f 100644
--- a/ttnn/cpp/ttnn/operations/matmul/device/kernels/compute/bmm_large_block_zm_fused_bias_activation.cpp
+++ b/ttnn/cpp/ttnn/operations/matmul/device/kernels/compute/bmm_large_block_zm_fused_bias_activation.cpp
@@ -14,6 +14,7 @@
 #include "api/compute/bcast.h"
 #endif
 
+#include "api/debug/dprint.h"
 #include "api/compute/eltwise_unary/sfpu_split_includes.h"
 
 // Please update
@@ -200,6 +201,20 @@ void kernel_main() {
 
     constexpr bool spill = num_blocks_inner_dim > 1;
 
+    DPRINT_MATH(
+        DPRINT << "[bmm_fused] kernel_start in0_block_w=" << in0_block_w
+               << " in0_num_subblocks=" << in0_num_subblocks
+               << " in1_num_subblocks=" << in1_num_subblocks
+               << " num_blocks_inner_dim=" << num_blocks_inner_dim
+               << " num_blocks_w_dim=" << num_blocks_w_dim
+               << " num_blocks_h_dim=" << num_blocks_h_dim
+               << " out_subblock_h=" << out_subblock_h
+               << " out_subblock_w=" << out_subblock_w
+               << " batch=" << batch
+               << " spill=" << static_cast<uint32_t>(spill)
+               << ENDL();
+    )
+
     mm_block_init(
         in0_cb_id, in1_cb_id, mm_partials_cb_id, in1_transpose_tile, out_subblock_w, out_subblock_h, in0_block_w);
     for (uint32_t b = 0; b < batch; b++) {
@@ -216,6 +231,12 @@ void kernel_main() {
 
         for (uint32_t bh = 0; bh < num_blocks_h_dim; ++bh) {
             for (uint32_t bw = 0; bw < num_blocks_w_dim; ++bw) {
+                if (b == 0 && bh == 0 && bw == 0) {
+                    DPRINT_MATH(
+                        DPRINT << "[bmm_fused] enter_output_block bw=" << bw
+                               << " bh=" << bh << ENDL();
+                    )
+                }
                 bool enable_reload = false;
                 uint32_t out_num_tiles_to_wait = out_subblock_num_tiles;
 
@@ -232,6 +253,7 @@ void kernel_main() {
 
                 for (uint32_t block = 0; block < num_blocks_inner_dim; block++) {
                     bool last_out = block == (num_blocks_inner_dim - 1);
+                    const bool trace_first_block = (b == 0 && bh == 0 && bw == 0 && block == 0);
 // Configure packer once for pack out without Bias
 #if not defined FUSE_BIAS and defined PACK_RELU
                     if (last_out) {
@@ -241,6 +263,11 @@ void kernel_main() {
 #endif
 
                     if constexpr (in0_transpose_tile) {
+                        if (trace_first_block) {
+                            DPRINT_MATH(
+                                DPRINT << "[bmm_fused] transpose_begin" << ENDL();
+                            )
+                        }
                         transpose_wh_init_short(in0_transpose_cb_id);
                         PACK((pack_reconfig_data_format(in0_cb_id)));
 #ifdef PACKER_L1_ACC
@@ -250,10 +277,26 @@ void kernel_main() {
                         mm_block_init_short(
                             in0_cb_id, in1_cb_id, in1_transpose_tile, out_subblock_w, out_subblock_h, in0_block_w);
                         PACK((pack_reconfig_data_format(mm_partials_cb_id)));
+                        if (trace_first_block) {
+                            DPRINT_MATH(
+                                DPRINT << "[bmm_fused] transpose_done" << ENDL();
+                            )
+                        }
                     }
 
+                    if (trace_first_block) {
+                        DPRINT_MATH(
+                            DPRINT << "[bmm_fused] wait_in0_in1 begin in0_tiles=" << in0_block_num_tiles
+                                   << " in1_tiles=" << in1_block_num_tiles << ENDL();
+                        )
+                    }
                     cb_wait_front(in0_cb_id, in0_block_num_tiles);
                     cb_wait_front(in1_cb_id, in1_block_num_tiles);
+                    if (trace_first_block) {
+                        DPRINT_MATH(
+                            DPRINT << "[bmm_fused] wait_in0_in1 done" << ENDL();
+                        )
+                    }
 
                     int in0_index_subblock_offset = 0;
                     for (uint32_t in0_subblock = 0; in0_subblock < in0_num_subblocks; in0_subblock++) {
@@ -261,6 +304,11 @@ void kernel_main() {
                         for (uint32_t in1_subblock = 0; in1_subblock < in1_num_subblocks; in1_subblock++) {
                             tile_regs_acquire();
                             if (enable_reload) {
+                                if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                    DPRINT_MATH(
+                                        DPRINT << "[bmm_fused] reload_begin" << ENDL();
+                                    )
+                                }
                                 reload_from_cb_to_dst(
                                     in0_cb_id,
                                     in1_cb_id,
@@ -270,6 +318,11 @@ void kernel_main() {
                                     out_subblock_w,
                                     out_subblock_h,
                                     in0_block_w);
+                                if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                    DPRINT_MATH(
+                                        DPRINT << "[bmm_fused] reload_done" << ENDL();
+                                    )
+                                }
                             }
 
 #ifndef SKIP_COMPUTE
@@ -279,6 +332,11 @@ void kernel_main() {
                             uint32_t in0_index = in0_index_subblock_offset;  // offset into in0 block
                             uint32_t in1_index = in1_index_subblock_offset;  // offset into in1 block
                             // inner dim that we accumulate is the inner dim of in0/in1, which is in0_block_w
+                            if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                DPRINT_MATH(
+                                    DPRINT << "[bmm_fused] matmul_begin" << ENDL();
+                                )
+                            }
                             for (uint32_t inner_dim_idx = 0; inner_dim_idx < in0_block_w; ++inner_dim_idx) {
                                 // matmul outer product of (out_subblock_h x out_subblock_w) tiles that fill dst
                                 // accumulation is done by iterating matmul_block across inner dim
@@ -298,6 +356,11 @@ void kernel_main() {
                                 in1_index += in1_block_w;  // to stride down by 1 need to stride by in_per_core_w
                                                            // (should be called in1_block_w)
                             }
+                            if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                DPRINT_MATH(
+                                    DPRINT << "[bmm_fused] matmul_done" << ENDL();
+                                )
+                            }
 
 #endif  // SKIP_COMPUTE
 
@@ -310,6 +373,11 @@ void kernel_main() {
 #endif
                                 tile_regs_commit();
                                 // Pack out to output buffer
+                                if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                    DPRINT_MATH(
+                                        DPRINT << "[bmm_fused] pack_final_begin" << ENDL();
+                                    )
+                                }
                                 cb_reserve_back(mm_out_cb_id, out_subblock_num_tiles);
                                 tile_regs_wait();
 
@@ -334,14 +402,30 @@ void kernel_main() {
 
                                 tile_regs_release();
                                 cb_push_back(mm_out_cb_id, out_subblock_num_tiles);
+                                if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                    DPRINT_MATH(
+                                        DPRINT << "[bmm_fused] pack_final_done" << ENDL();
+                                    )
+                                }
 
                             } else {
                                 tile_regs_commit();
                                 // Wait for tiles in output buffer to be written out since interm and output share
                                 // memory
                                 if (block == 0) {
+                                    if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                        DPRINT_MATH(
+                                            DPRINT << "[bmm_fused] spill_wait_begin out_tiles=" << out_num_tiles_to_wait
+                                                   << ENDL();
+                                        )
+                                    }
                                     cb_reserve_back(out_cb_id, out_num_tiles_to_wait);
                                     out_num_tiles_to_wait += out_subblock_num_tiles;
+                                    if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                        DPRINT_MATH(
+                                            DPRINT << "[bmm_fused] spill_wait_done" << ENDL();
+                                        )
+                                    }
                                 }
                                 // Move partial result to interm buffer
                                 cb_reserve_back(mm_partials_cb_id, out_subblock_num_tiles);
@@ -364,6 +448,11 @@ void kernel_main() {
 
                                 tile_regs_release();
                                 cb_push_back(mm_partials_cb_id, out_subblock_num_tiles);
+                                if (trace_first_block && in0_subblock == 0 && in1_subblock == 0) {
+                                    DPRINT_MATH(
+                                        DPRINT << "[bmm_fused] spill_pack_done" << ENDL();
+                                    )
+                                }
                             }
 
                             in1_index_subblock_offset += out_subblock_w;
@@ -402,6 +491,11 @@ void kernel_main() {
                 }
 
 #ifdef FUSE_BIAS
+                if (b == 0 && bh == 0 && bw == 0) {
+                    DPRINT_MATH(
+                        DPRINT << "[bmm_fused] fuse_bias_begin" << ENDL();
+                    )
+                }
 #ifdef PACK_RELU
                 // if last block we pack the final result with relu enabled
                 PACK((llk_pack_relu_config(ReluType::ZERO_RELU)));
@@ -460,6 +554,11 @@ void kernel_main() {
                 if constexpr (num_blocks_w_dim > 1) {
                     cb_pop_front(bias_cb_id, in1_block_w);
                 }
+                if (b == 0 && bh == 0 && bw == 0) {
+                    DPRINT_MATH(
+                        DPRINT << "[bmm_fused] fuse_bias_done" << ENDL();
+                    )
+                }
 #endif  // FUSE_BIAS
                 if constexpr (untilize_out) {
 #ifdef PACK_RELU
@@ -497,4 +596,7 @@ void kernel_main() {
             }
         }
     }
+    DPRINT_MATH(
+        DPRINT << "[bmm_fused] kernel_done" << ENDL();
+    )
 }
diff --git a/ttnn/cpp/ttnn/operations/matmul/device/kernels/dataflow/reader_bmm_tile_layout_in0_sender_padding.cpp b/ttnn/cpp/ttnn/operations/matmul/device/kernels/dataflow/reader_bmm_tile_layout_in0_sender_padding.cpp
index fb7dfe7c26..486bd30beb 100644
--- a/ttnn/cpp/ttnn/operations/matmul/device/kernels/dataflow/reader_bmm_tile_layout_in0_sender_padding.cpp
+++ b/ttnn/cpp/ttnn/operations/matmul/device/kernels/dataflow/reader_bmm_tile_layout_in0_sender_padding.cpp
@@ -5,6 +5,7 @@
 #include <stdint.h>
 
 #include "api/dataflow/dataflow_api.h"
+#include "api/debug/dprint.h"
 #include "hostdevcommon/common_values.hpp"
 #include "ttnn/operations/ccl/kernel_common/worker_sync_utils.hpp"
 #include "ttnn/operations/kernel_helper_functions/pad_tile.hpp"
@@ -161,12 +162,17 @@ void kernel_main() {
                 if constexpr (get_batch_from_reader) {
 #ifndef SKIP_MCAST
                     // First broadcast this to other cores
+                    DPRINT << "[in0_sender] batch_valid_wait_begin b=" << b << " bB=" << bB
+                           << " need=" << in0_mcast_num_dests << ENDL();
                     noc_semaphore_wait(in0_mcast_sender_semaphore_addr_ptr, in0_mcast_num_dests);
+                    DPRINT << "[in0_sender] batch_valid_wait_done b=" << b << " bB=" << bB << ENDL();
                     noc_semaphore_set(in0_mcast_sender_semaphore_addr_ptr, 0);
                     noc_semaphore_set(in0_mcast_receiver_semaphore_addr_ptr, is_batch_valid ? VALID : IGNORE_BATCH);
                     noc_semaphore_set_multicast(
                         in0_mcast_receiver_semaphore_addr, in0_mcast_receiver_semaphore_noc_addr, in0_mcast_num_cores);
                     noc_async_writes_flushed();
+                    DPRINT << "[in0_sender] batch_valid_flag_flushed b=" << b << " bB=" << bB
+                           << " is_valid=" << is_batch_valid << ENDL();
                     // Reset the semaphore value to VALID
                     noc_semaphore_set(in0_mcast_receiver_semaphore_addr_ptr, VALID);
 #endif  // SKIP_MCAST
@@ -283,7 +289,15 @@ void kernel_main() {
                         // wait until all in0 mcast destinations have atomically incremented the in0 semaphore_addr
                         // (i.e. its value should be in0_mcast_num_dests), then reset the semaphore_addr value back to
                         // zero for the next block
+                        if (b == 0 && bB == 0 && bh == 0 && bw == 0 && block < 2) {
+                            DPRINT << "[in0_sender] block_wait_begin block=" << block
+                                   << " need=" << in0_mcast_num_dests
+                                   << " bytes=" << in0_block_size_bytes << ENDL();
+                        }
                         noc_semaphore_wait(in0_mcast_sender_semaphore_addr_ptr, in0_mcast_num_dests);
+                        if (b == 0 && bB == 0 && bh == 0 && bw == 0 && block < 2) {
+                            DPRINT << "[in0_sender] block_wait_done block=" << block << ENDL();
+                        }
                         noc_semaphore_set(in0_mcast_sender_semaphore_addr_ptr, 0);
 
                         // Now we have the block in the CB address, we can mcast to dests!
@@ -296,6 +310,9 @@ void kernel_main() {
                             in0_block_size_bytes,
                             in0_mcast_num_cores,
                             true);
+                        if (b == 0 && bB == 0 && bh == 0 && bw == 0 && block < 2) {
+                            DPRINT << "[in0_sender] block_mcast_issued block=" << block << ENDL();
+                        }
 
                         // Note: no need for write barrier, since these two multicasts are done on the same noc id, same
                         // vc, same cmd_buf Also, this only works because we are setting VCs statically (using
@@ -312,6 +329,9 @@ void kernel_main() {
                             in0_mcast_receiver_semaphore_addr,
                             in0_mcast_receiver_semaphore_noc_addr,
                             in0_mcast_num_cores);
+                        if (b == 0 && bB == 0 && bh == 0 && bw == 0 && block < 2) {
+                            DPRINT << "[in0_sender] block_flag_issued block=" << block << ENDL();
+                        }
 #endif  // SKIP_MCAST
 
                         // Common for sharded and interleaved paths
@@ -339,7 +359,9 @@ void kernel_main() {
             in0_tensor_start_tile_id += MtKt;
         }
     }
+    DPRINT << "[in0_sender] final_write_barrier_begin" << ENDL();
     noc_async_write_barrier();
+    DPRINT << "[in0_sender] final_write_barrier_done" << ENDL();
     // For completeness, we empty the sparsity CB if it was reserved earlier
     if constexpr (batchB > 0) {
         cb_push_back(cb_id_sparsity, 1);
